program HelloPauPatrol;
vars
name[30] : char;
i , len : int;

main(){
    len = 0;
    read(name[len]);
    while(name[len]!=' ') do {
        len += 1;
        read(name[len]);
    } 
    
    write("Welcome to the crew ");
    from i = 0 to (len-1) do {
        write(name[i]);
    }
}